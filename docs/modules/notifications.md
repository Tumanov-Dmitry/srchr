# Уведомления

## Простыми словами

Уведомления сообщают пользователю, что важного произошло с его задачами, материалами, мероприятиями, организациями и другими объектами.

## Зачем нужен модуль

Пользователь не должен постоянно проверять все разделы вручную. Уведомление приводит его к нужному действию: посмотреть отклик, результат модерации или приглашение.

## Что можно делать

- увидеть количество новых уведомлений в колокольчике;
- открыть историю за последние 30 дней;
- перейти к связанному объекту;
- отметить одно или все уведомления прочитанными;
- настроить интересующие типы событий;
- отдельно настроить In-App, будущий email и Telegram;
- администраторам — видеть события модерации и системные ошибки.

## Назначение

Модуль централизует продуктовые и системные события. In-App является рабочим каналом; email и Telegram заложены в настройках для будущей реализации.

## Сущности

- `notification_events` — системный журнал произошедших событий.
- `notifications` — уведомления конкретных получателей.
- настройки уведомлений — предпочтения пользователя по типам и каналам.

## Поведение

- Колокольчик показывает число непрочитанных и последние записи.
- Полная история доступна на `/notifications` и в dashboard.
- Переход к источнику не отмечает запись автоматически.
- Кнопка подтверждения отмечает конкретное уведомление прочитанным.
- Ссылка определяется типом и идентификатором источника, а не хостом из пользовательского ввода.

## Реализация

- `lib/notifications.ts`
- `lib/notification-targets.ts`
- `lib/supabase/notification-queries.ts`
- `components/notifications/notification-bell.tsx`
- `components/notifications/notification-read-button.tsx`
- `app/actions/notifications.ts`
- `supabase/sql/create-notifications.sql`

## Администраторы

Администраторы получают события модерации и системные ошибки. Отдельные представления находятся в `/admin/notifications` и `/admin/system-events`.
